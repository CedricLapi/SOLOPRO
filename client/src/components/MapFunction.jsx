import React from 'react'

const MapFunction = () => {

    const name = "Yousef";
    const arr = [1, 2, 3, 4, 5];

    arr.map(x => console.log(x));

  return (
    <div>

    </div>
  )
}

export default MapFunction